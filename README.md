# Valentine's Day site

Open `index.html` in your browser to view the page.

Files added/updated:
- `index.html` — main page
- `styles.css` — responsive styles and animations
- `script.js` — floating-heart interaction (already present)
- `assets/heart.svg` — heart asset used on the page

To test locally (Windows):

```powershell
# from workspace folder
start index.html
```
